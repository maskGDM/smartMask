<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartmask">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.SmartMask">
        <activity
            android:name=".DeleteMask"
            android:exported="true" />
        <activity android:name=".AirQualityTable" />
        <activity android:name=".ListMaskActivity" />
        <activity android:name=".AirQuiality" />
        <activity android:name=".Profile1" />
        <activity android:name=".Options" />
        <activity android:name=".Settings" />
        <activity android:name=".Menu" />
        <activity android:name=".ConfigureValues" />
        <activity android:name=".ModalInformation" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>